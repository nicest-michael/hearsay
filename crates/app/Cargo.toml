[package]
name = "hearsay-app"
description = "Hearsay application/orchestration layer (depends only on the core ports)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
hearsay-core.workspace = true

[lints.rust]
unsafe_code = "forbid"
